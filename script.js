var input = require('fs').readFileSync('./stdin', 'utf8');
var lines = input.split('\n');
var columns = input.split(' ');


var n = parseInt(lines.shift());
columns.shift()


for (let i = 0; i<n; i++){
    var f1 = parseInt(lines.shift())
    var f2 = parseInt(columns.shift())

    /*
    Resolva o exercício aqui
    */
}